export default function Skills() {
    return (
        <section id="skills" className="min-h-screen flex items-center justify-center border-b border-gray-800">
            <h2 className="text-3xl font-bold">Skills (SSR)</h2>
        </section>
    );
}
