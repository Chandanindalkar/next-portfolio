export default function Contact() {
    return (
        <section id="contact" className="min-h-screen flex items-center justify-center border-b border-gray-800">
            <h2 className="text-3xl font-bold">Contact (SSR)</h2>
        </section>
    );
}
